steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'us-central1-docker.pkg.dev/$PROJECT_ID/github.com/senthilmsbk/cloudtest/pipe-firstsample:$COMMIT_SHA', '.']
images:
  - 'us-central1-docker.pkg.dev/$PROJECT_ID/YOUR_REPO/YOUR_IMAGE:$COMMIT_SHA'
options:
  logging: CLOUD_LOGGING_ONLY
